#pragma once
#include <vector>
#include "subject.h"

double compareDims(std::vector<Subject> subjectSet) { //TODO: Move to relations.cpp
	for (int i = 0; i < subjectSet.size(); i++) {
		std::cout << "<compareDims> size of array " << (i + 1) << " : " << (subjectSet.at(i)).
	}
}

double getDistance(std::vector<Subject> subjectArray) {
	
}